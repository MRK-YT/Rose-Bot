# SUNAIF ADKAR

<img src="https://telegra.ph/file/7a027595c903f8cb9b35b.png" width="150" height="150"><br>


```
from tg_bot.sample_config import Config


class Development(Config):
    OWNER_ID = 1249529870  # my telegram ID
    OWNER_USERNAME = "sunaif_adkar"  # my telegram username
    API_KEY = "your bot api key"  # my api key, as provided by the botfather
    SQLALCHEMY_DATABASE_URI = 'postgresql://username:password@localhost:5432/database'(false) # sample db credentials
    MESSAGE_DUMP = '-1234567890'-(add -100)- # some group chat that your bot is a member of
    USE_MESSAGE_DUMP = True
    MO_TECH_YT = [subscribers] Subscribers Now # @sabotschannel@Movie_addictzs_channel@ME_NewMovies#Support
    SUDO_USERS = [7338096026, 7338096026]  # List of id's for users which have sudo access to the bot.
    LOAD = [12345678910]
    NO_LOAD = ['translation'] # translation stop
```

